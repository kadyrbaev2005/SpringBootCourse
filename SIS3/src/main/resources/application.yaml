spring:
  application:
    name: SIS3
  datasource:
    url: jdbc:postgresql://localhost:5433/sis3db
    username: postgres
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect